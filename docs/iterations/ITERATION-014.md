# Iteration 014: 治理 skill 弱模型闭环执行防呆

> 文档状态：Closed
> 计划发布日期：2026-05-27
> 计划目标：将治理 skill 从依赖模型自行收口的指导文本，强化为具有显式完成判定、
> 状态同步和残余登记要求的执行协议。
>
> 基线保护：本文件一旦提交，以下“发布计划基线”内容不可因实施或改线而覆写；
> 同目标执行只向执行区追加事实，换目标必须保留本页并新建 iteration 编号。

## 发布计划基线：目标

当前治理 skill 能指导模型创建结构和发现问题，但能力较弱的使用者可能在生成局部
产物后过早报完成。本轮将闭环要求放入主入口，并提供可机械遵循的协议与评估场景。

## 发布计划基线：候选故事与依赖

| ID | 标题 | 父 Epic | 当前状态 | 启动条件 |
|----|------|---------|----------|----------|
| EVO-037 | 治理 skill 弱模型闭环执行防呆 | 无 | Done | 用户指出 skill 使用者闭环能力不足 |

## 发布计划基线：不做事项

- 不改 Evolith 业务代码或生产运行配置。
- 不把模型能力问题伪装成必然可由校验脚本完全解决的承诺。
- 不提交或清理外部 skill 仓库已有未提交内容。

## 发布计划基线：计划验收标准

- [x] skill 主入口具有实施任务不可跳过的闭环门禁。
- [x] 专门参考文档提供按步骤执行的闭环协议、台账和交付模板。
- [x] skill 的评估场景覆盖“局部完成后错误宣称完成”。
- [x] 项目和 skill 的文档/结构验证取得通过结果。

## 发布计划基线：计划验证

```bash
git diff --check
# 检查项目文档相对链接
# 运行 agent-project-governance skill 结构校验
```

## 发布计划基线：风险与回滚

| 风险 | 计划控制 |
|------|----------|
| 主 skill 文本膨胀导致触发后负担过大 | 主入口只放强制门禁，细节拆到单层 reference |
| 覆盖 skill 仓库并行修改 | 使用隔离副本合并，只发布本轮涉及文件 |

## 实际激活与执行记录

| 日期 | 类型 | 记录 |
|------|------|------|
| 2026-05-27 | activation | 用户指出使用 skill 的模型能力可能有限，需要提高问题闭环可靠性。 |
| 2026-05-27 | design | 将 `complete` 收紧为产物、状态、证据、残余和交付五维闭合；缺项只能报告 `partial` 或 `blocked`。 |
| 2026-05-27 | skill | 在主入口加入强制闭环契约，新增 `closure-protocol.md`，并更新引导、方法论、结构契约与过早完成评估用例。 |
| 2026-05-27 | validation | 项目 Markdown 链接及 `git diff --check` 通过；skill `quick_validate.py`、Markdown 链接及 `git diff --check` 通过。 |
