# EVO-062 sqlx 0.7→0.8/0.9 迁移（解决 future-incompat）

## Required Reads

- [Product Backlog](../PRODUCT-BACKLOG.md)

## Summary

| Field | Value |
| --- | --- |
| ID | EVO-062 |
| Type | tech-debt |
| Status | Proposed |
| Priority | P1 |
| Parent Epic | None recorded |
| Source | Iteration 032 依赖审计暂缓项 |

## Problem Or Outcome

**19 个 repo 文件 / ~120 call sites** 依赖 `query!` / `query_as!` 宏与 `FromRow` derive；0.7→0.8 是 macro 与 `Database` trait 跨主版本破坏；已观测 `sqlx-postgres 0.7.4` never-type-fallback warning（Rust 2024 edition 强制前不阻断），是 Iteration 028 显式归口

## Goal And Non-Goals

- Goal: deliver the outcome described by the title and decision context without expanding scope during implementation.
- Non-goals: do not implement adjacent backlog items unless they are listed as hard dependencies in this file.

## Dependencies And Blockers

- No hard blocker recorded in the source backlog.

## Governing ADRs, Specs Or Decisions

- None recorded beyond the product backlog and item file.

## Acceptance Criteria

- 类型：tech-debt
- 优先级：P1
- 状态：Proposed
- 来源：Iteration 032 依赖审计暂缓项 / Iteration 028 显式归口 / 2026-06-01
- 用户/工程价值：解决 `sqlx-postgres 0.7.4` never-type-fallback future-incompat warning（Rust 2024 edition 强制前不阻断，但 1.95+ 已可见）；获取 0.8 引入的 `Database` trait 简化与新 `Executor` 抽象。
- 范围：升级 `sqlx = "0.7"` → `"0.8"`（暂不升 0.9，0.9 仍是 alpha）；19 个 repo 文件的 `query!` / `query_as!` 宏按 0.8 语法适配；`FromRow` derive 重新生成；SQLite + PostgreSQL 双轨 integration tests 全绿。
- 不做：不升级 sqlx 0.9（alpha）；不修改业务查询语义；不重新设计 repository trait。
- 验收标准：`cargo check / clippy / test --workspace` 0 error；`future-incompat` 报告 0 sqlx-postgres 警告。
- 影响范围：所有 8 个 `infra/src/db/{pg_,}*_repo.rs` + 2 个 integration test 文件。
- 最小验证方式：跑 `cargo test --workspace`（含 PG integration tests）；`cargo report future-incompatibilities` 无 sqlx 项。

## Validation Evidence Required

- Record real command output or manual evidence in the selected iteration before marking this item Done.
- Run checks matching the changed surface from [Testing SOP](../../sop/TESTING.md).

## Residual Work Destination

- Update this file and `docs/backlog/PRODUCT-BACKLOG.md` if scope, status, dependencies or Required Reads change.
- Move completed or deferred execution context to `docs/backlog/archive/<period>/` during backlog compaction.

## Source Snapshot

- Source: Iteration 032 依赖审计暂缓项
- Decision context: **19 个 repo 文件 / ~120 call sites** 依赖 `query!` / `query_as!` 宏与 `FromRow` derive；0.7→0.8 是 macro 与 `Database` trait 跨主版本破坏；已观测 `sqlx-postgres 0.7.4` never-type-fallback warning（Rust 2024 edition 强制前不阻断），是 Iteration 028 显式归口
