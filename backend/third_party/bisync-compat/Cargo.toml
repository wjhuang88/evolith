[package]
name = "bisync"
version = "0.3.2"
edition = "2021"
publish = false
license = "MIT OR Apache-2.0"
description = "Compatibility shim for gix-protocol 0.64 using the maintained bisync2 fork"

[dependencies]
bisync2 = "=0.3.2"
